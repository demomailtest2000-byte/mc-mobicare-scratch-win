import{prisma}from'@/lib/prisma';import{Experience}from'@/components/Experience';export const dynamic='force-dynamic';export default async function P(){if(!process.env.DATABASE_URL)return <main><img className="logo" src="/brand-logo.png" alt="MC Mobicare Mobiles & Electronics"/><section className="card"><p className="eyebrow">MC MOBICARE</p><h1>Campaign unavailable</h1><p className="sub">Scratch &amp; Win is being prepared. Please ask a store team member for assistance.</p></section></main>;const all=await prisma.setting.findMany();return <Experience settings={Object.fromEntries(all.map((x:{key:string,value:string})=>[x.key,x.value]))}/>}
