import{NextRequest,NextResponse}from'next/server';import QRCode from'qrcode';import{admin}from'@/lib/auth';export async function GET(req:NextRequest){if(!await admin())return NextResponse.json({error:'Unauthorized'},{status:401});const base=(process.env.NEXT_PUBLIC_APP_URL||req.nextUrl.origin).replace(/\/$/,'');const url=`${base}/scratch`;const png=await QRCode.toDataURL(url,{width:1200,margin:3,errorCorrectionLevel:'H',color:{dark:'#1627a8',light:'#ffffff'}});return NextResponse.json({url,png})}
