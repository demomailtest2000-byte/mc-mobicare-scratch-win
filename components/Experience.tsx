'use client';import{useState}from'react';import{BRANDS,RANGES}from'@/lib/prizes';export function Experience({settings}:{settings:Record<string,string>}){const[s,setS]=useState(0);const[e,setE]=useState('');const[r,setR]=useState<any>();async function go(f:FormData){setE('');const q=await fetch('/api/participation',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),phone:f.get('phone'),invoiceNumber:f.get('invoiceNumber'),brand:f.get('brand'),priceRange:f.get('range')})});const j=await q.json();if(!q.ok)return setE(j.error);setR(j);setS(1)}return <main><img className="logo" src="/brand-logo.png" alt="MC Mobicare Mobiles & Electronics"/>{s===0&&<section className="card"><p className="eyebrow">SHOP & GET A SURPRISE GIFT</p><h1>SCRATCH & WIN</h1><p className="sub">{settings.campaignSubtitle||'Congratulations! You may have a special gift waiting for you.'}</p><div className="trust"><div><b>1 Customer = 1 Chance</b><br/>One entry per mobile number and invoice.</div><div><b>Scratch once</b><br/>Reveal your gift.</div></div><form action={go}><label className="field">FULL NAME<input name="name" required minLength={2}/></label><label className="field">CONTACT NUMBER<input name="phone" required inputMode="numeric"/></label><label className="field">INVOICE NUMBER<input name="invoiceNumber" required maxLength={64} placeholder="Enter your bill / invoice number" autoCapitalize="characters"/></label><label className="field">BRAND<select name="brand" required defaultValue=""><option value="" disabled>Select brand</option>{BRANDS.map(x=><option key={x}>{x}</option>)}</select></label><label className="field">PRICE RANGE<select name="range" required defaultValue=""><option value="" disabled>Select range</option>{RANGES.map(x=><option key={x}>{x}</option>)}</select></label>{e&&<p className="error">{e}</p>}<button className="button">CONTINUE TO SCRATCH</button><p className="notice">Your details are collected for Scratch & Win participation and campaign records.</p></form></section>}{s===1&&<section className="card"><p className="eyebrow">STEP 2 — SCRATCH TO REVEAL</p><div className="scratch"><p>🎁 YOUR GIFT<br/>{r.prize}</p><button className="overlay" onClick={()=>setS(2)}>SCRATCH HERE<br/><small>Tap to reveal</small></button></div></section>}{s===2&&<section className="card"><p className="eyebrow">🎉 CONGRATULATIONS!</p><h1>Your gift</h1><div className="scratch"><p>{r.prize}</p></div><p className="sub">Please show this screen to our store staff to collect your gift.</p><div className="claim">{r.claimId}</div><p className="notice">Save this Claim ID for reference.</p><div className="social"><a href={settings.instagramUrl||'#'} target="_blank" rel="noreferrer">📸 FOLLOW US ON INSTAGRAM</a><a href={settings.googleReviewUrl||'#'} target="_blank" rel="noreferrer">⭐ GIVE US A GOOGLE REVIEW</a><a href={settings.whatsappUrl||'#'} target="_blank" rel="noreferrer">💬 CHAT ON WHATSAPP</a><a href={settings.youtubeUrl||'#'} target="_blank" rel="noreferrer">▶️ WATCH US ON YOUTUBE</a><a href={settings.facebookUrl||'#'} target="_blank" rel="noreferrer">📘 FOLLOW US ON FACEBOOK</a></div></section>}</main>}
