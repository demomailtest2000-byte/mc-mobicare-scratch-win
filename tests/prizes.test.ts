import{expect,it}from'vitest';import{prizeFor}from'../lib/prizes';it('applies all requested overrides',()=>{expect(prizeFor('OPPO','₹0 - ₹14,999')).toBe('Sipper Bottle');expect(prizeFor('Samsung','₹15,000 - ₹24,999')).toContain('Tiffin');expect(prizeFor('Apple','₹55,000 and above')).toBe('Backpack + Chopper + Tiffin Box');expect(prizeFor('Laptop','₹0 - ₹14,999')).toContain('Keyboard')})
